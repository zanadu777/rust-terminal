<UserControl x:Class="RustTerminal.RecentCommandsBrowser"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Recent Commands"
                   FontSize="14"
                   FontWeight="Bold"
                   Margin="0,0,0,8" />

        <DataGrid Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  ItemsSource="{Binding RecentCommandsData}"
                  SelectedItem="{Binding SelectedCommand, Mode=TwoWay}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Command" Binding="{Binding CommandText}" Width="250" />
                <DataGridTextColumn Header="Directory" Binding="{Binding DirectoryPath}" Width="350" />
                <DataGridTextColumn Header="Last Used" Binding="{Binding ExecutedUtc, StringFormat=yyyy-MM-dd HH:mm:ss}" Width="150" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="Delete Selected" Command="{Binding DeleteSelectedCommand}" Margin="0,0,8,0" />
            <Button Content="Delete by Directory..." Command="{Binding DeleteByDirectoryCommand}" Margin="0,0,8,0" />
            <Button Content="Clear All" Command="{Binding ClearAllCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
