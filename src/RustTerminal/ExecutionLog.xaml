<UserControl x:Class="RustTerminal.ExecutionLog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:RustTerminal"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="8" />
            <RowDefinition Height="2*" />
        </Grid.RowDefinitions>

        <DataGrid x:Name="ExecutionGrid"
                  Grid.Row="0"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  ItemsSource="{Binding Source, RelativeSource={RelativeSource AncestorType={x:Type local:ExecutionLog}}}"
                  SelectedItem="{Binding SelectedExecution, RelativeSource={RelativeSource AncestorType={x:Type local:ExecutionLog}}, Mode=TwoWay}"
                  Margin="0,0,0,8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Command" Binding="{Binding Command}" Width="*" MinWidth="120"/>
                <DataGridTextColumn x:Name="StartColumn" Header="Start" Binding="{Binding Start, StringFormat=yyyy-MM-dd HH:mm:ss}" Width="160" />
                <DataGridTextColumn x:Name="StopColumn" Header="Completed" Binding="{Binding Stop, StringFormat=yyyy-MM-dd HH:mm:ss}" Width="160" />
                <DataGridTextColumn x:Name="DurationColumn" Header="Duration" Binding="{Binding Duration, StringFormat=hh\\:mm\\:ss}" Width="100" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBox Grid.Row="2"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 Text="{Binding SelectedExecution.ResponseText, RelativeSource={RelativeSource AncestorType={x:Type local:ExecutionLog}}, Mode=OneWay}" />
    </Grid>
</UserControl>
